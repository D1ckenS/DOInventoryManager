<Window x:Class="DOInventoryManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DO Inventory Manager" 
        Height="800" 
        Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized">

    <Window.Resources>
        <!-- Button Styles -->
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#34495e"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="20,15"/>
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="Center"
                                            Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#3498db"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#2980b9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ActiveNavButtonStyle" TargetType="Button" BasedOn="{StaticResource NavButtonStyle}">
            <Setter Property="Background" Value="#3498db"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="30"/>
            <!-- Status Bar -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2c3e50">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0">
                    <TextBlock Text="⛽" FontSize="24" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <TextBlock Text="DO Inventory Manager" 
                             FontSize="20" 
                             FontWeight="Bold" 
                             Foreground="White" 
                             VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,20,0">
                    <TextBlock x:Name="CurrentDateText" 
                             FontSize="12" 
                             Foreground="LightGray" 
                             VerticalAlignment="Center" 
                             Margin="0,0,20,0"/>
                    <Button Content="📊 Generate Report" 
                          Background="#27ae60" 
                          Foreground="White" 
                          BorderThickness="0" 
                          Padding="15,10" 
                          Click="GenerateReport_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <!-- Navigation -->
                <ColumnDefinition Width="*"/>
                <!-- Content -->
            </Grid.ColumnDefinitions>

            <!-- Navigation Panel -->
            <Border Grid.Column="0" Background="#34495e" BorderBrush="#2c3e50" BorderThickness="0,0,1,0">
                <StackPanel Margin="0,20,0,0">
                    <Button x:Name="DashboardBtn" Content="📊 Dashboard" Style="{StaticResource ActiveNavButtonStyle}" Click="Dashboard_Click"/>
                    <Button x:Name="SuppliersBtn" Content="🏢 Suppliers" Style="{StaticResource NavButtonStyle}" Click="Suppliers_Click"/>
                    <Button x:Name="VesselsBtn" Content="🚢 Vessels" Style="{StaticResource NavButtonStyle}" Click="Vessels_Click"/>
                    <Button x:Name="PurchasesBtn" Content="🛒 Purchase Entry" Style="{StaticResource NavButtonStyle}" Click="Purchases_Click"/>
                    <Button x:Name="ConsumptionBtn" Content="⛽ Consumption Entry" Style="{StaticResource NavButtonStyle}" Click="Consumption_Click"/>
                    <Button x:Name="TripsBtn" Content="🗺️ Trip Management" Style="{StaticResource NavButtonStyle}" Click="Trips_Click"/>
                    <Button x:Name="AllocationBtn" Content="📋 Monthly Allocation" Style="{StaticResource NavButtonStyle}" Click="Allocation_Click"/>
                    <Button x:Name="SummaryBtn" Content="📈 Monthly Summary" Style="{StaticResource NavButtonStyle}" Click="Summary_Click"/>

                    <!-- Spacer -->
                    <Rectangle Height="30" Fill="Transparent"/>

                    <!-- Utilities -->
                    <Button x:Name="BackupBtn" Content="💾 Backup Data" Style="{StaticResource NavButtonStyle}" Click="Backup_Click"/>
                    <Button x:Name="SettingsBtn" Content="⚙️ Settings" Style="{StaticResource NavButtonStyle}" Click="Settings_Click"/>
                </StackPanel>
            </Border>

            <!-- Content Area -->
            <Border Grid.Column="1" Background="White" Padding="20">
                <Frame x:Name="ContentFrame" NavigationUIVisibility="Hidden"/>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2" Background="#ecf0f1" BorderBrush="#bdc3c7" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="StatusText" 
                         Grid.Column="0" 
                         Text="Ready" 
                         VerticalAlignment="Center" 
                         Margin="10,0" 
                         FontSize="12"/>

                <TextBlock x:Name="DatabaseStatus" 
                         Grid.Column="1" 
                         Text="Database: Connected" 
                         VerticalAlignment="Center" 
                         Margin="10,0" 
                         FontSize="12" 
                         Foreground="Green"/>

                <TextBlock x:Name="VersionText" 
                         Grid.Column="2" 
                         Text="v1.0.0" 
                         VerticalAlignment="Center" 
                         Margin="10,0" 
                         FontSize="12" 
                         Foreground="Gray"/>
            </Grid>
        </Border>
    </Grid>
</Window>